# aws-ec2-step-snapshots-describe

This [AWS EBS](https://aws.amazon.com/ebs/) step container lists the EBS snapshots
in an AWS region and sets an output, `snapshots`, to an array containing information about them.